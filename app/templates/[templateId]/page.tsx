import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="min-h-screen w-full flex justify-center items-center text-2xl font-bold">
      Template Preview
    </div>
  );
};

export default page;
